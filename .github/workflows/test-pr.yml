---
name: Call Validate PR Workflow
on:
  pull_request:
    branches: [main]
    # Temporarily remove path filter to test
    # paths:
    #   - 'src/pages/**'

jobs:
  call_validation:  # Fixed typo: vall → call
    name: Validate PR
    uses: AdobeDocs/adp-devsite-workflow/.github/workflows/validate-pr.yml@DEVSITE-1978-linter-bug
